<resources>

    <style name="Splash" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowBackground">@drawable/splash_screen</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="QuestionBarButtons" parent="Widget.AppCompat.Button.Borderless">
        <item name="android:elevation">0dp</item>
        <item name="android:minWidth">100dp</item>
        <item name="android:layout_marginStart">@dimen/kau_padding_small</item>
        <item name="android:layout_marginEnd">@dimen/kau_padding_small</item>
    </style>

    <style name="ChoiceButtons">
        <item name="android:layout_marginStart">@dimen/kau_spacing_huge</item>
        <item name="android:layout_marginEnd">@dimen/kau_spacing_huge</item>
        <item name="android:paddingTop">@dimen/kau_padding_normal</item>
        <item name="android:paddingBottom">@dimen/kau_padding_normal</item>
    </style>
</resources>
